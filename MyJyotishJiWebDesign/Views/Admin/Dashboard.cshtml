
@{
    Layout = "AdminLayout";
}
<style>
    .card-body {
        padding-left: 15px !important;
        padding-right: 15px !important;
    }

    label {
        display: none;
    }
</style>
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<link href="https://cdn.datatables.net/2.1.3/css/dataTables.dataTables.min.css" rel="stylesheet" />
<script src="https://cdn.datatables.net/2.1.3/js/dataTables.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />

<!-- Sale & Revenue Start -->
<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-sm-6 col-xl-3">
            <div class="bg-light rounded d-flex align-items-center justify-content-between p-3">
                <img src="~/gif/team.png" height="70" width="70" />
          @*       <img src="~/gif/about.png" height="70" width="70" /> *@
                <div class="ms-3">
                    <p class="mb-2">Total Users</p>
                    <h6 id="totalUser" class="mb-0">3636</h6>
                </div>
            </div>
        </div>
    
        <div class="col-sm-6 col-xl-3">
            <div class="bg-light rounded d-flex align-items-center justify-content-between p-3">
                <img src="~/gif/management.png" height="70" width="70" />
                <div class="ms-3">
                    <p class="mb-2">Total Jyotish</p>
                    <h6 id="totalJyotish" class="mb-0">446</h6>
                </div>
            </div>
        </div>
        <div class="col-sm-6 col-xl-3">
            <div class="bg-light rounded d-flex align-items-center justify-content-between p-3">
                <img src="~/gif/clock.png" height="70" width="70" />
                <div class="ms-3">
                    <p class="mb-2">Pending Jyotish</p>
                    <h6 id="pendingJyotish" class="mb-0">345</h6>
                </div>
            </div>
        </div>
        <div class="col-sm-6 col-xl-3">
            <div class="bg-light rounded d-flex align-items-center justify-content-between p-3">
                <img src="~/gif/employee.png" height="70" width="70" />
                <div class="ms-3">
                    <p class="mb-2"> Rejected Jyotish<p>
                        <h6 id="rejectedJyotish" class="mb-0">1234</h6>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Sale & Revenue End -->

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-sm-12 col-xl-6">
            <div class="bg-light text-center rounded p-4">
                <div class="d-flex align-items-center justify-content-between mb-4">
                    <h6 class="mb-0">Total Customers</h6>
                    <a href="">Show All</a>
                </div>
                <canvas id="worldwide-sales"></canvas>
            </div>
        </div>
        <div class="col-sm-12 col-xl-6">
            <div class="bg-light text-center rounded p-4">
                <div class="d-flex align-items-center justify-content-between mb-4">
                    <h6 class="mb-0">Total Customer</h6>
                    <a href="">Show All</a>
                </div>
                <canvas id="salse-revenue"></canvas>
            </div>
        </div>
    </div>
</div>
<!-- Sales Chart End -->



<div class="container-fluid mt-4">
    <div class="card">

        <div class="d-flex flex-row justify-content-between align-items-center pt-2 px-4">
            <h5 class="text-primary">Pooja Category</h5>
            <a href="/Admin/AddPoojaCategory">
                <button class="btn btn-primary">Add New Category</button>
            </a>
        </div>
        <hr />

        <div class="card-body">
            <div class="table-responsive">
                <table id="DataTable" class="table table-bordered table-striped">
                    <!-- Table content goes here -->
                </table>
            </div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function () {
        function fetchData() {
            const Token = localStorage.getItem("Token");
            $.ajax({
                url: "https://api.myjyotishg.in/api/Admin/Appointment",
                type: 'GET',
                headers: {
                    'Authorization': `Bearer ${localStorage.getItem("Token")}`, // Example of an authorization header
                    'Content-Type': 'application/json', // Example of a content type header

                },
                success: function (result) {
                    let htmls = '<thead><tr><th>Date Time</th><th>Name</th><th>Amount</th><th>Problem</th><th>Solution</th></tr></thead><tbody>';

                    $.each(result.data, function (key, item) {
                        htmls += '<tr>';
                        htmls += `<td class="text-center">${item.dateTime}</td>`;
                        htmls += `<td class="text-center">${item.name}</td>`;
                        htmls += `<td class="text-center">${item.amount}</td>`;
                        htmls += `<td class="text-center">${item.problem}</td>`;
                        htmls += `<td class="text-center">${item.solution}</td>`;

                        htmls += '</tr>';
                    });

                    htmls += '</tbody>';
                    $('#DataTable').html(htmls);
                    $('#DataTable').DataTable();
                },
                error: function (errormessage) {
                    console.error('Error fetching data:', errormessage.responseText);
                }
            });
        }
        // let table = new DataTable('#DataTable');
        // Load all records by default
        fetchData();
        function DashValues() {
            $.ajax({
                url: "https://api.myjyotishg.in/api/Admin/Dashboard",
                type: 'GET',
                headers: {
                    'Authorization': `Bearer ${localStorage.getItem("Token")}`, // Example of an authorization header
                    'Content-Type': 'application/json', // Example of a content type header

                },
                success: function (result) {
                    let User = `${result.data.totalUsers}`;
                    let Jyotish = `${result.data.totalJyotish}`;
                    let PendingJyotish = `${result.data.pendingJyotish}`;
                    let RejectedJyotish = `${result.data.rejectedJyotish}`;

                    $('#totalUser').html(User);
                    $('#totalJyotish').html(Jyotish);
                    $('#pendingJyotish').html(PendingJyotish);
                    $('#rejectedJyotish').html(RejectedJyotish);
                  
                },
                error: function (errormessage) {
                    console.error('Error fetching data:', errormessage.responseText);
                }
            });
        }
        DashValues();

    });

</script>