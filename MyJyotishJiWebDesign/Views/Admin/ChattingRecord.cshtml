@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = "AdminLayout";
}

<link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.min.css">
<!-- Include jQuery -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<!-- Include DataTables JS -->
<script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
<style>
    .card-body {
        padding-left: 15px !important;
        padding-right: 15px !important;
    }
</style>

<link href="https://cdn.datatables.net/2.1.3/css/dataTables.dataTables.min.css" rel="stylesheet" />
<script src="https://cdn.datatables.net/2.1.3/js/dataTables.min.js"></script>

<div class="container-fluid mt-4">
    <div class="card">

        <div class="d-flex flex-row justify-content-between align-items-center pt-2 px-4">
            <h5 class="text-primary">Chatting Records</h5>
            <a href="/Admin/AddPoojaCategory">
            </a>

        </div>
        <hr />

        <div class="card-body">
            <div class="table-responsive">
                <table id="DataTable" class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>DateTime</th>

                            <th>Message</th>
                            <th>UserId</th>
                            <th>JyotishId</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Data will be populated here by JavaScript -->
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@* <style>
/*     td, th {
        white-space: nowrap; /* Prevent text from wrapping */
    }

  
</style>

<div class="container-fluid pt-4 px-4">
    <div class="bg-light text-center rounded p-4">

        <div class="d-flex align-items-center justify-content-between mb-4">
            <h6 class="mb-0">Chatting Records</h6>

        </div>   
        <table id="DataTable" class="table table-striped text-start align-middle table-bordered table-hover mb-0" style="width:100%;" ">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>DateTime</th>
              
                    <th>Message</th>
                    <th>UserId</th>
                    <th>JyotishId</th>
                </tr>
            </thead>
            <tbody>
                <!-- Data will be populated here by JavaScript -->
            </tbody>
        </table>
    </div>
</div>

 */ *@

<script>
    $(document).ready(function () {
        function fetchData() {
            $.ajax({
                url: "https://api.myjyotishg.in/api/Admin/ChattingRecord",
                type: 'GET',
                headers: {
                    'Authorization': `Bearer ${localStorage.getItem("Token")}`, // Example of an authorization header
                    'Content-Type': 'application/json'
                },
                success: function (result) {
                    // Destroy the existing DataTable instance if it exists
                    if ($.fn.DataTable.isDataTable('#DataTable')) {
                        $('#DataTable').DataTable().clear().destroy();
                    }

                    // Create a DataTable and populate it with data
                    $('#DataTable').DataTable({
                        data: result.data,
                        columns: [
                            { data: 'id', title: 'Id' },
                            { data: 'dateTime', title: 'DateTime' },
                            { data: 'message', title: 'Message' },
                            { data: 'userId', title: 'UserId' },
                            { data: 'jyotishId', title: 'JyotishId' }
                        ],
                        // Additional options can be configured here
                    });
                },
                error: function (errormessage) {
                    console.error('Error fetching data:', errormessage.responseText);
                }
            });
        }

        // Load all records by default
        fetchData();
    });
</script>